import{createClient}from'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2.105.0/+esm';
import{loadAdminAccess,canView,canEdit,accessLevel}from'./admin-access.js';
const sb=createClient('https://ltjdhconuiqblxfjzpzj.supabase.co','sb_publishable_-7SIdrqTmUibqpA7mXXrgg_SHz9EJRr',{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}});
let lastUser='';
function info(){const f=(location.pathname.split('/').pop()||'').toLowerCase(),q=new URLSearchParams(location.search);if(f==='content-admin.html'||f==='index.html'){const v=q.get('view')||'questions';return{section:v==='trainings'?'trainings':v==='structure'?'structure':'questions',needsEdit:v==='newQuestion',view:v}}if(f==='students-admin.html'||f==='student-detail-admin.html')return{section:'students',view:'students'};if(f==='sessions-admin.html')return{section:'class_sessions',view:q.get('view')||'entry'};if(f==='assets-admin.html')return{section:'question_assets',view:'assets'};if(f==='reviews-admin.html')return{section:'essay_reviews',view:'reviews'};return null}
function injectStyle(css){let x=document.getElementById('adminPermissionStyle');if(!x){x=document.createElement('style');x.id='adminPermissionStyle';document.head.appendChild(x)}x.textContent=css}
function applyReadOnly(meta){document.body.classList.add('adminViewOnly');let css='.adminViewOnly .permissionEditOnly{display:none!important}';if(meta.section==='questions'){if(meta.view==='questions')css+=' .adminViewOnly .editQ,.adminViewOnly .archiveQ{display:none!important}';}
if(meta.section==='trainings'){css+=' .adminViewOnly #newTrainingBtn,.adminViewOnly #saveTrainingBtn,.adminViewOnly #resetTrainingBtn,.adminViewOnly .archiveTraining{display:none!important}';setTimeout(()=>document.querySelectorAll('#trainingEditor input,#trainingEditor select,#trainingEditor textarea').forEach(x=>x.disabled=true),0)}
if(meta.section==='structure'){css+=' .adminViewOnly #saveLessonBtn,.adminViewOnly #saveTopicBtn{display:none!important}';setTimeout(()=>document.querySelectorAll('#structureView input,#structureView select,#structureView textarea').forEach(x=>x.disabled=true),0)}
if(meta.section==='students'){css+=' .adminViewOnly .reset,.adminViewOnly .toggle{display:none!important}';setTimeout(()=>{const a=document.getElementById('createBtn')?.closest('.card');const b=document.getElementById('bulkBtn')?.closest('.card');a?.classList.add('hidden');b?.classList.add('hidden')},0)}
if(meta.section==='class_sessions'){css+=' .adminViewOnly #newTab,.adminViewOnly #newSessionBtn,.adminViewOnly #saveBtn,.adminViewOnly #reloadStudents{display:none!important}';setTimeout(()=>document.querySelectorAll('#entryView input,#entryView select,#entryView textarea').forEach(x=>x.disabled=true),0)}
if(meta.section==='question_assets'){css+=' .adminViewOnly .del{display:none!important}';setTimeout(()=>{document.getElementById('upload')?.closest('.card')?.classList.add('hidden')},0)}
if(meta.section==='essay_reviews'){css+=' .adminViewOnly #save,.adminViewOnly #useAi,.adminViewOnly .quick{display:none!important}';setTimeout(()=>{['grade','feedback'].forEach(id=>{const x=document.getElementById(id);if(x)x.disabled=true})},0)}
injectStyle(css)}
async function apply(session){const meta=info();if(!meta||!session?.user||session.user.app_metadata?.role!=='admin')return;if(lastUser===session.user.id&&document.documentElement.dataset.adminGuardDone==='1')return;lastUser=session.user.id;let ctx;try{ctx=await loadAdminAccess(sb)}catch{location.replace('./');return}if(!canView(ctx,meta.section)){location.replace('./');return}if(meta.needsEdit&&!canEdit(ctx,meta.section)){location.replace('content-admin.html?view=questions');return}if(meta.section==='class_sessions'&&meta.view==='entry'&&!canEdit(ctx,'class_sessions')){location.replace('sessions-admin.html?view=history');return}document.documentElement.dataset.adminAccess=accessLevel(ctx,meta.section);document.documentElement.dataset.adminGuardDone='1';if(!canEdit(ctx,meta.section))applyReadOnly(meta)}
const{data:{session}}=await sb.auth.getSession();await apply(session);sb.auth.onAuthStateChange((_e,s)=>{setTimeout(()=>apply(s),0)});
if((location.pathname.split('/').pop()||'').toLowerCase()==='reviews-admin.html'){
  import('./reviews-admin-history-fix.js').catch(e=>console.warn('reviews history fix',e));
  import('./reviews-answer-images.js').catch(e=>console.warn('reviews answer images',e));
}
